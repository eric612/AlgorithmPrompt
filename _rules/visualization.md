## Visualization Rule

Every major algorithm stage should have a visual output when possible.

Examples:

```text
input frame / signal
intermediate processing result
output frame / signal
error map / difference image
histogram / distribution
block grid / heat map
```

Use simple and clear visualization first.

Do not optimize visualization before the algorithm behavior is understandable.
