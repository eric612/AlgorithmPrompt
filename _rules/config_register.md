## Config Register Rule

For RTL-oriented work, list config registers:

| Register | Bits | Type | Default | Shadow | Update Timing | Description |
|---|---:|---|---:|---|---|---|

Every runtime parameter must be included.

Shadow behavior must be explicit: immediate, line boundary, frame
boundary, commit bit, or double-buffered config.
